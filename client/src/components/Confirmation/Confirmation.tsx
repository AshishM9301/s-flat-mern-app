import React from "react";

type Props = {
  title: string;
  details?: string;
  actionButton: () => void;
  closeUpButton: () => void;
};

const Confirmation = (props: Props) => {
  return <div></div>;
};

export default Confirmation;
